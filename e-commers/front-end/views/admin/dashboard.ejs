<!-- ejs -->
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" href="../public/img/57589.png" type="image/x-icon">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <title>Admin Dashboard</title>
</head>

<body style="background-color: rgba(128, 128, 128, 0.515);">

  <!-- Navbar and Headline -->

  <div class="container mt-3">
    <div class="container d-flex justify-content-center align-items-center">
      <%- include('../partials/navbar') %>
    </div>
    <div class="container d-flex justify-content-center align-items-center text-center mt-5">
      <h1 style="font-size: 40px; color: red; text-decoration: solid; font-weight: bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000, 2px 2px 5px rgb(75, 75, 75);">Admin Dashboard</h1>
    </div>
  </div>

  <!-- Navigation tabs for different sections -->

  <div class="container mt-3">
    <ul class="container d-flex justify-content-evenly align-content-center nav nav-tabs mt-4" id="adminTabs" role="tablist">
      <li class="nav-item" role="presentation" style="border-radius:20px ;">
        <button class="nav-link active" id="products-tab" data-bs-toggle="tab" data-bs-target="#products" type="button" role="tab" aria-controls="products" aria-selected="true" style="border-radius:20px ;background-color: rgb(79, 74, 74); color: red; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Products</button>
      </li>
      <li class="nav-item" role="presentation" style="border-radius:20px ;">
        <button class="nav-link" id="categories-tab" data-bs-toggle="tab" data-bs-target="#categories" type="button" role="tab" aria-controls="categories" aria-selected="false" style="border-radius:20px ;background-color: rgb(79, 74, 74); color: red; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Categories</button>
      </li>
      <li class="nav-item" role="presentation" style="border-radius:20px ;">
        <button class="nav-link" id="brands-tab" data-bs-toggle="tab" data-bs-target="#brands" type="button" role="tab" aria-controls="brands" aria-selected="false" style="border-radius:20px ;background-color: rgb(79, 74, 74); color: red; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Brands</button>
      </li>
      <li class="nav-item" role="presentation" style="border-radius:20px ;">
        <button class="nav-link" id="orders-tab" data-bs-toggle="tab" data-bs-target="#orders" type="button" role="tab" aria-controls="orders" aria-selected="false" style="border-radius:20px ;background-color: rgb(79, 74, 74); color: red; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;"">Orders</button>
      </li>
      <li class="nav-item" role="presentation" style="border-radius:20px ;">
        <button class="nav-link" id="users-tab" data-bs-toggle="tab" data-bs-target="#users" type="button" role="tab" aria-controls="users" aria-selected="false" style="border-radius:20px ;background-color: rgb(79, 74, 74); color: red; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Users</button>
      </li>
      <li class="nav-item" role="presentation" style="border-radius:20px ;">
        <button class="nav-link" id="memberships-tab" data-bs-toggle="tab" data-bs-target="#memberships" type="button" role="tab" aria-controls="memberships" aria-selected="false" style="border-radius:20px ;background-color: rgb(79, 74, 74); color: red; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Memberships</button>
      </li>
    </ul>
      <div class="tab-content" id="adminTabsContent">
      </div>
  </div>



  <!-- TABS-->

  <!-- PRODUCTS TAB -->
  <div class="tab-pane fade show active" id="products" role="tabpanel" aria-labelledby="products-tab">
        <div class="card mt-3" style="background-color: rgb(79, 74, 74); color: red; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000; border-radius: 15px;">
          <div class="card-header d-flex justify-content-between align-items-center">
            <h3>Product Management</h3>
            <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#addProductModal" style="border-radius:20px ;color: red; border: 1px solid black; font-weight: bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">
              Add New Product
            </button>
          </div>
          <div class="card-body">
            <!-- Product search -->
            <div class="row mb-4">
              <div class="col-md-12">
                <h4>Advanced Search</h4>
                <form id="advancedSearchForm" class="row g-3">
                  <div class="col-md-6">
                    <input type="text" id="searchQuery" class="form-control" placeholder="Search term..." 
                          style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                  </div>
                  <div class="col-md-3">
                    <select id="searchType" class="form-select" 
                            style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                      <option value="name">Product Name</option>
                      <option value="category">Category</option>
                      <option value="brand">Brand</option>
                    </select>
                  </div>
                  <div class="col-md-3">
                    <button type="submit" class="btn btn-danger w-100" 
                            style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">
                      Search Products
                    </button>
                  </div>
                </form>
              </div>
            </div>
            
            <div id="searchResults" class="mt-3" style="display: none;">
              <div class="alert" style="background-color: rgba(128, 128, 128, 0.515); color: red; border: 1px solid black;">
                <span id="resultCount"></span> products found
                <button type="button" class="btn-close float-end" aria-label="Close" id="clearSearchResults"></button>
              </div>
            </div>

            <!-- Products table -->
            <div class="table-responsive">
              <% if (typeof products !== 'undefined' && products.length > 0) { %>
                <table class="table table-dark table-hover">
                  <thead>
                    <tr>
                      <th>ID</th>
                      <th>Name</th>
                      <th>Description</th>
                      <th>Price</th>
                      <th>Quantity</th>
                      <th>Brand</th>
                      <th>Category</th>
                      <th width="150">Actions</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% products.forEach(product => { %>
                      <tr>
                        <td><%= product.id %></td>
                        <td><%= product.name %></td>
                        <td><%= product.description.length > 30 ? product.description.substring(0, 30) + '...' : product.description %></td>
                        <td>$<%= product.price.toFixed(2) %></td>
                        <td><%= product.quantity %></td>
                        <td><%= product.brand ? product.brand.name : 'Unknown' %></td>
                        <td><%= product.category ? product.category.name : 'Unknown' %></td>
                        <td>
                          <div class="btn-group btn-group-sm" role="group">
                            <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#editProductModal<%= product.id %>">
                              <i class="bi bi-pencil"></i> Edit
                            </button>
                            <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteProductModal<%= product.id %>">
                              <i class="bi bi-trash"></i> Delete
                            </button>
                          </div>

                          <!-- Edit Product Modal -->
                          <div class="modal fade" id="editProductModal<%= product.id %>" tabindex="-1" aria-labelledby="editProductModalLabel<%= product.id %>" aria-hidden="true">
                            <div class="modal-dialog">
                              <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
                                <div class="modal-header">
                                  <h5 class="modal-title" id="editProductModalLabel<%= product.id %>">Edit Product</h5>
                                  <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                </div>
                                <div class="modal-body">
                                  <form action="/products/edit/<%= product.id %>" method="POST">
                                    <div class="mb-3">
                                      <label for="name<%= product.id %>" class="form-label">Name</label>
                                      <input type="text" class="form-control" id="name<%= product.id %>" name="name" value="<%= product.name %>" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                                    </div>
                                    <div class="mb-3">
                                      <label for="description<%= product.id %>" class="form-label">Description</label>
                                      <textarea class="form-control" id="description<%= product.id %>" name="description" rows="3" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;"><%= product.description %></textarea>
                                    </div>
                                    <div class="mb-3">
                                      <label for="price<%= product.id %>" class="form-label">Price</label>
                                      <input type="number" class="form-control" id="price<%= product.id %>" name="price" step="0.01" value="<%= product.price %>" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                                    </div>
                                    <div class="mb-3">
                                      <label for="quantity<%= product.id %>" class="form-label">Quantity</label>
                                      <input type="number" class="form-control" id="quantity<%= product.id %>" name="quantity" value="<%= product.quantity %>" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                                    </div>
                                    <div class="mb-3">
                                      <label for="imgUrl<%= product.id %>" class="form-label">Image URL</label>
                                      <input type="text" class="form-control" id="imgUrl<%= product.id %>" name="imgUrl" value="<%= product.imgUrl %>" style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                                    </div>
                                    <div class="mb-3">
                                      <label for="brandId<%= product.id %>" class="form-label">Brand</label>
                                      <select class="form-select" id="brandId<%= product.id %>" name="brandId" style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                                        <% if (locals.brands && brands.length > 0) { %>
                                          <% brands.forEach(brand => { %>
                                            <option value="<%= brand.id %>" <%= product.brandId === brand.id ? 'selected' : '' %>><%= brand.name %></option>
                                          <% }) %>
                                        <% } else { %>
                                          <option value="">No brands available</option>
                                        <% } %>
                                      </select>
                                    </div>
                                    <div class="mb-3">
                                      <label for="categoryId<%= product.id %>" class="form-label">Category</label>
                                      <select class="form-select" id="categoryId<%= product.id %>" name="categoryId" style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                                        <% if (locals.categories && categories.length > 0) { %>
                                          <% categories.forEach(category => { %>
                                            <option value="<%= category.id %>" <%= product.categoryId === category.id ? 'selected' : '' %>><%= category.name %></option>
                                          <% }) %>
                                        <% } else { %>
                                          <option value="">No categories available</option>
                                        <% } %>
                                      </select>
                                    </div>
                                    <div class="text-end">
                                      <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                      <button type="submit" class="btn btn-danger" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Save Changes</button>
                                    </div>
                                  </form>
                                </div>
                              </div>
                            </div>
                          </div>

                          <!-- Delete Product Modal -->
                          <div class="modal fade" id="deleteProductModal<%= product.id %>" tabindex="-1" aria-labelledby="deleteProductModalLabel<%= product.id %>" aria-hidden="true">
                            <div class="modal-dialog">
                              <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
                                <div class="modal-header">
                                  <h5 class="modal-title" id="deleteProductModalLabel<%= product.id %>">Confirm Delete</h5>
                                  <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                </div>
                                <div class="modal-body">
                                  <p>Are you sure you want to delete the product "<%= product.name %>"?</p>
                                </div>
                                <div class="modal-footer">
                                  <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                  <form action="/products/delete/<%= product.id %>" method="POST">
                                    <button type="submit" class="btn btn-danger" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Delete</button>
                                  </form>
                                </div>
                              </div>
                            </div>
                          </div>
                        </td>
                      </tr>
                    <% }) %>
                  </tbody>
                </table>
              <% } else { %>
                <div class="alert alert-info" style="background-color: rgba(128, 128, 128, 0.515); color: red; border: 1px solid black;">
                  No products found.
                </div>
              <% } %>
            </div>
          </div>
        </div>
  </div>

  <!-- CATEGORIES TAB -->
  <div class="tab-pane fade" id="categories" role="tabpanel" aria-labelledby="categories-tab">
  <div class="card mt-3" style="background-color: rgb(79, 74, 74); color: red; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000; border-radius: 15px;">
    <div class="card-header d-flex justify-content-between align-items-center">
      <h3>Category Management</h3>
      <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#addCategoryModal" style="color: red; border: 1px solid black; font-weight: bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">
        Add New Category
      </button>
    </div>
    <div class="card-body">
      <!-- Category search -->
      <div class="row mb-3">
        <div class="col-md-6">
          <div class="input-group">
            <input type="text" id="categorySearch" class="form-control" placeholder="Search categories..." style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
            <button class="btn btn-danger" type="button" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">
              Search
            </button>
          </div>
        </div>
      </div>

      <!-- Categories table -->
      <div class="table-responsive">
        <% if (locals.categories && categories.length > 0) { %>
          <table class="table table-dark table-hover">
            <thead>
              <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Product Count</th>
                <th width="150">Actions</th>
              </tr>
            </thead>
            <tbody>
              <% categories.forEach(category => { %>
                <tr>
                  <td><%= category.id %></td>
                  <td><%= category.name %></td>
                  <td><%= category.productCount || 0 %></td>
                  <td>
                    <div class="btn-group btn-group-sm" role="group">
                      <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#editCategoryModal<%= category.id %>">
                        <i class="bi bi-pencil"></i> Edit
                      </button>
                      <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteCategoryModal<%= category.id %>">
                        <i class="bi bi-trash"></i> Delete
                      </button>
                    </div>

                    <!-- Edit Category Modal -->
                    <div class="modal fade" id="editCategoryModal<%= category.id %>" tabindex="-1" aria-labelledby="editCategoryModalLabel<%= category.id %>" aria-hidden="true">
                      <div class="modal-dialog">
                        <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
                          <div class="modal-header">
                            <h5 class="modal-title" id="editCategoryModalLabel<%= category.id %>">Edit Category</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                          </div>
                          <div class="modal-body">
                            <form action="/categories/edit/<%= category.id %>" method="POST">
                              <div class="mb-3">
                                <label for="name<%= category.id %>" class="form-label">Name</label>
                                <input type="text" class="form-control" id="name<%= category.id %>" name="name" value="<%= category.name %>" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                              </div>
                              <div class="text-end">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                <button type="submit" class="btn btn-danger" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Save Changes</button>
                              </div>
                            </form>
                          </div>
                        </div>
                      </div>
                    </div>

                    <!-- Delete Category Modal -->
                    <div class="modal fade" id="deleteCategoryModal<%= category.id %>" tabindex="-1" aria-labelledby="deleteCategoryModalLabel<%= category.id %>" aria-hidden="true">
                      <div class="modal-dialog">
                        <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
                          <div class="modal-header">
                            <h5 class="modal-title" id="deleteCategoryModalLabel<%= category.id %>">Confirm Delete</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                          </div>
                          <div class="modal-body">
                            <p>Are you sure you want to delete the category "<%= category.name %>"?</p>
                            <% if (category.productCount && category.productCount > 0) { %>
                              <div class="alert alert-warning" style="background-color: rgba(255, 193, 7, 0.2); color: red; border: 1px solid #ffc107;">
                                Warning: This category has <%= category.productCount %> products associated with it. Deleting this category may affect these products.
                              </div>
                            <% } %>
                          </div>
                          <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                            <form action="/categories/delete/<%= category.id %>" method="POST">
                              <button type="submit" class="btn btn-danger" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Delete</button>
                            </form>
                          </div>
                        </div>
                      </div>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        <% } else { %>
          <div class="alert alert-info" style="background-color: rgba(128, 128, 128, 0.515); color: red; border: 1px solid black;">
            No categories found.
          </div>
        <% } %>
      </div>
    </div>
  </div>
  </div>

  <!-- BRANDS TAB -->
  <div class="tab-pane fade" id="brands" role="tabpanel" aria-labelledby="brands-tab">
  <div class="card mt-3" style="background-color: rgb(79, 74, 74); color: red; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000; border-radius: 15px;">
    <div class="card-header d-flex justify-content-between align-items-center">
      <h3>Brand Management</h3>
      <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#addBrandModal" style="color: red; border: 1px solid black; font-weight: bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">
        Add New Brand
      </button>
    </div>
    <div class="card-body">
      <!-- Brand search -->
      <div class="row mb-3">
        <div class="col-md-6">
          <div class="input-group">
            <input type="text" id="brandSearch" class="form-control" placeholder="Search brands..." style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
            <button class="btn btn-danger" type="button" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">
              Search
            </button>
          </div>
        </div>
      </div>

      <!-- Brands table -->
      <div class="table-responsive">
        <% if (locals.brands && brands.length > 0) { %>
          <table class="table table-dark table-hover">
            <thead>
              <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Product Count</th>
                <th width="150">Actions</th>
              </tr>
            </thead>
            <tbody>
              <% brands.forEach(brand => { %>
                <tr>
                  <td><%= brand.id %></td>
                  <td><%= brand.name %></td>
                  <td><%= brand.productCount || 0 %></td>
                  <td>
                    <div class="btn-group btn-group-sm" role="group">
                      <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#editBrandModal<%= brand.id %>">
                        <i class="bi bi-pencil"></i> Edit
                      </button>
                      <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteBrandModal<%= brand.id %>">
                        <i class="bi bi-trash"></i> Delete
                      </button>
                    </div>

                    <!-- Edit Brand Modal -->
                    <div class="modal fade" id="editBrandModal<%= brand.id %>" tabindex="-1" aria-labelledby="editBrandModalLabel<%= brand.id %>" aria-hidden="true">
                      <div class="modal-dialog">
                        <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
                          <div class="modal-header">
                            <h5 class="modal-title" id="editBrandModalLabel<%= brand.id %>">Edit Brand</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                          </div>
                          <div class="modal-body">
                            <form action="/brands/edit/<%= brand.id %>" method="POST">
                              <div class="mb-3">
                                <label for="name<%= brand.id %>" class="form-label">Name</label>
                                <input type="text" class="form-control" id="name<%= brand.id %>" name="name" value="<%= brand.name %>" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                              </div>
                              <div class="text-end">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                <button type="submit" class="btn btn-danger" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Save Changes</button>
                              </div>
                            </form>
                          </div>
                        </div>
                      </div>
                    </div>

                    <!-- Delete Brand Modal -->
                    <div class="modal fade" id="deleteBrandModal<%= brand.id %>" tabindex="-1" aria-labelledby="deleteBrandModalLabel<%= brand.id %>" aria-hidden="true">
                      <div class="modal-dialog">
                        <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
                          <div class="modal-header">
                            <h5 class="modal-title" id="deleteBrandModalLabel<%= brand.id %>">Confirm Delete</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                          </div>
                          <div class="modal-body">
                            <p>Are you sure you want to delete the brand "<%= brand.name %>"?</p>
                            <% if (brand.productCount && brand.productCount > 0) { %>
                              <div class="alert alert-warning" style="background-color: rgba(255, 193, 7, 0.2); color: red; border: 1px solid #ffc107;">
                                Warning: This brand has <%= brand.productCount %> products associated with it. Deleting this brand may affect these products.
                              </div>
                            <% } %>
                          </div>
                          <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                            <form action="/brands/delete/<%= brand.id %>" method="POST">
                              <button type="submit" class="btn btn-danger" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Delete</button>
                            </form>
                          </div>
                        </div>
                      </div>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        <% } else { %>
          <div class="alert alert-info" style="background-color: rgba(128, 128, 128, 0.515); color: red; border: 1px solid black;">
            No brands found.
          </div>
        <% } %>
      </div>
    </div>
  </div>
  </div>

  <!-- ORDERS TAB -->
  <div class="tab-pane fade" id="orders" role="tabpanel" aria-labelledby="orders-tab">
  <div class="card mt-3" style="background-color: rgb(79, 74, 74); color: red; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000; border-radius: 15px;">
    <div class="card-header d-flex justify-content-between align-items-center">
      <h3>Order Management</h3>
      <div class="input-group" style="max-width: 300px;">
        <input type="text" id="orderSearch" class="form-control" placeholder="Search orders..." style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
        <button class="btn btn-danger" type="button" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">
          Search
        </button>
      </div>
    </div>
    <div class="card-body">
      <!-- Filter options -->
      <div class="row mb-3">
        <div class="col-md-6">
          <div class="btn-group" role="group">
            <button type="button" class="btn btn-outline-danger active" data-filter="all" style="color: red; border: 1px solid black; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">All Orders</button>
            <button type="button" class="btn btn-outline-danger" data-filter="paid" style="color: red; border: 1px solid black; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Paid</button>
            <button type="button" class="btn btn-outline-danger" data-filter="shipped" style="color: red; border: 1px solid black; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Shipped</button>
            <button type="button" class="btn btn-outline-danger" data-filter="delivered" style="color: red; border: 1px solid black; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Delivered</button>
            <button type="button" class="btn btn-outline-danger" data-filter="canceled" style="color: red; border: 1px solid black; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Canceled</button>
          </div>
        </div>
      </div>

      <!-- Orders table -->
      <div class="table-responsive">
        <% if (locals.orders && orders.length > 0) { %>
          <table class="table table-dark table-hover">
            <thead>
              <tr>
                <th>Order ID</th>
                <th>User</th>
                <th>Date</th>
                <th>Status</th>
                <th>Total Amount</th>
                <th>Items</th>
                <th width="180">Actions</th>
              </tr>
            </thead>
            <tbody>
              <% orders.forEach(order => { %>
                <tr data-status="<%= order.status.toLowerCase() %>">
                  <td><%= order.id %></td>
                  <td><%= order.user ? `${order.user.firstname} ${order.user.lastname}` : 'Unknown User' %></td>
                  <td><%= new Date(order.createdAt).toLocaleDateString() %></td>
                  <td>
                    <span class="badge rounded-pill <%= 
                      order.status === 'paid' ? 'bg-info' : 
                      order.status === 'shipped' ? 'bg-primary' : 
                      order.status === 'delivered' ? 'bg-success' : 
                      order.status === 'canceled' ? 'bg-danger' : 
                      'bg-secondary' 
                    %>">
                      <%= order.status %>
                    </span>
                  </td>
                  <td>$<%= order.totalAmount.toFixed(2) %></td>
                  <td><%= order.OrderItems ? order.OrderItems.length : 0 %></td>
                  <td>
                    <div class="btn-group btn-group-sm" role="group">
                      <button type="button" class="btn btn-info" data-bs-toggle="modal" data-bs-target="#viewOrderModal<%= order.id %>">
                        <i class="bi bi-eye"></i> View
                      </button>
                      <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#updateOrderModal<%= order.id %>">
                        <i class="bi bi-pencil"></i> Update
                      </button>
                    </div>

                    <!-- View Order Modal -->
                    <div class="modal fade" id="viewOrderModal<%= order.id %>" tabindex="-1" aria-labelledby="viewOrderModalLabel<%= order.id %>" aria-hidden="true">
                      <div class="modal-dialog modal-lg">
                        <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
                          <div class="modal-header">
                            <h5 class="modal-title" id="viewOrderModalLabel<%= order.id %>">Order Details #<%= order.id %></h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                          </div>
                          <div class="modal-body">
                            <div class="row mb-3">
                              <div class="col-md-6">
                                <h6>Order Information</h6>
                                <p>
                                  <strong>Order ID:</strong> <%= order.id %><br>
                                  <strong>Date:</strong> <%= new Date(order.createdAt).toLocaleString() %><br>
                                  <strong>Status:</strong> <span class="badge <%= 
                                    order.status === 'paid' ? 'bg-info' : 
                                    order.status === 'shipped' ? 'bg-primary' : 
                                    order.status === 'delivered' ? 'bg-success' : 
                                    order.status === 'canceled' ? 'bg-danger' : 
                                    'bg-secondary' 
                                  %>"><%= order.status %></span>
                                </p>
                              </div>
                              <div class="col-md-6">
                                <h6>Customer Information</h6>
                                <% if (order.user) { %>
                                  <p>
                                    <strong>Name:</strong> <%= order.user.firstname %> <%= order.user.lastname %><br>
                                    <strong>Email:</strong> <%= order.user.email %><br>
                                    <% if (order.user.address) { %>
                                      <strong>Address:</strong> <%= order.user.address %><br>
                                    <% } %>
                                    <% if (order.user.phone) { %>
                                      <strong>Phone:</strong> <%= order.user.phone %>
                                    <% } %>
                                  </p>
                                <% } else { %>
                                  <p>Customer information not available</p>
                                <% } %>
                              </div>
                            </div>
                            
                            <h6>Order Items</h6>
                            <div class="table-responsive">
                              <% if (order.OrderItems && order.OrderItems.length > 0) { %>
                                <table class="table table-dark">
                                  <thead>
                                    <tr>
                                      <th>Product</th>
                                      <th>Price</th>
                                      <th>Quantity</th>
                                      <th>Subtotal</th>
                                    </tr>
                                  </thead>
                                  <tbody>
                                    <% order.OrderItems.forEach(item => { %>
                                      <tr>
                                        <td><%= item.Product ? item.Product.name : `Product ID: ${item.productId}` %></td>
                                        <td>$<%= item.price.toFixed(2) %></td>
                                        <td><%= item.quantity %></td>
                                        <td>$<%= (item.price * item.quantity).toFixed(2) %></td>
                                      </tr>
                                    <% }) %>
                                  </tbody>
                                  <tfoot>
                                    <% if (order.discountAmount && order.discountAmount > 0) { %>
                                      <tr>
                                        <td colspan="3" class="text-end">Discount:</td>
                                        <td>-$<%= order.discountAmount.toFixed(2) %></td>
                                      </tr>
                                    <% } %>
                                    <tr>
                                      <td colspan="3" class="text-end"><strong>Total:</strong></td>
                                      <td><strong>$<%= order.totalAmount.toFixed(2) %></strong></td>
                                    </tr>
                                  </tfoot>
                                </table>
                              <% } else { %>
                                <p>No items found for this order</p>
                              <% } %>
                            </div>
                            
                            <% 
                            // Extract payment details if available
                            let paymentDetails = {};
                            try {
                              if (order.paymentDetails) {
                                paymentDetails = JSON.parse(order.paymentDetails);
                              }
                            } catch (e) {
                              // Silently handle parse errors
                            }
                            %>
                            
                            <% if (paymentDetails && Object.keys(paymentDetails).length > 0) { %>
                              <h6>Payment Information</h6>
                              <p>
                                <% if (paymentDetails.transactionId) { %>
                                  <strong>Transaction ID:</strong> <%= paymentDetails.transactionId %><br>
                                <% } %>
                                <% if (paymentDetails.cardNumber) { %>
                                  <strong>Payment Method:</strong> Credit Card (**** **** **** <%= paymentDetails.cardNumber.slice(-4) %>)
                                <% } %>
                              </p>
                            <% } %>
                          </div>
                          <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                          </div>
                        </div>
                      </div>
                    </div>

                    <!-- Update Order Modal -->
                    <div class="modal fade" id="updateOrderModal<%= order.id %>" tabindex="-1" aria-labelledby="updateOrderModalLabel<%= order.id %>" aria-hidden="true">
                      <div class="modal-dialog">
                        <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
                          <div class="modal-header">
                            <h5 class="modal-title" id="updateOrderModalLabel<%= order.id %>">Update Order Status</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                          </div>
                          <div class="modal-body">
                            <form action="/orders/update/<%= order.id %>" method="POST">
                              <div class="mb-3">
                                <label for="status<%= order.id %>" class="form-label">Status</label>
                                <select class="form-select" id="status<%= order.id %>" name="status" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                                  <option value="paid" <%= order.status === 'paid' ? 'selected' : '' %>>Paid</option>
                                  <option value="shipped" <%= order.status === 'shipped' ? 'selected' : '' %>>Shipped</option>
                                  <option value="delivered" <%= order.status === 'delivered' ? 'selected' : '' %>>Delivered</option>
                                  <option value="canceled" <%= order.status === 'canceled' ? 'selected' : '' %>>Canceled</option>
                                </select>
                              </div>
                              <div class="text-end">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                <button type="submit" class="btn btn-danger" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Update Status</button>
                              </div>
                            </form>
                          </div>
                        </div>
                      </div>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        <% } else { %>
          <div class="alert alert-info" style="background-color: rgba(128, 128, 128, 0.515); color: red; border: 1px solid black;">
            No orders found.
          </div>
        <% } %>
      </div>
    </div>
  </div>
  </div>

  <!-- USERS TAB -->
  <div class="tab-pane fade" id="users" role="tabpanel" aria-labelledby="users-tab">
  <div class="card mt-3" style="background-color: rgb(79, 74, 74); color: red; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000; border-radius: 15px;">
    <div class="card-header d-flex justify-content-between align-items-center">
      <h3>User Management</h3>
      <div class="input-group" style="max-width: 300px;">
        <input type="text" id="userSearch" class="form-control" placeholder="Search users..." style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
        <button class="btn btn-danger" type="button" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">
          Search
        </button>
      </div>
    </div>
    <div class="card-body">
      <!-- Users table -->
      <div class="table-responsive">
        <% if (locals.users && users.length > 0) { %>
          <table class="table table-dark table-hover">
            <thead>
              <tr>
                <th>ID</th>
                <th>Username</th>
                <th>Name</th>
                <th>Email</th>
                <th>Role</th>
                <th>Membership</th>
                <th>Joined Date</th>
                <th width="150">Actions</th>
              </tr>
            </thead>
            <tbody>
              <% users.forEach(user => { %>
                <tr>
                  <td><%= user.id %></td>
                  <td><%= user.username %></td>
                  <td><%= user.firstname %> <%= user.lastname %></td>
                  <td><%= user.email %></td>
                  <td>
                    <span class="badge <%= user.roleId === 1 ? 'bg-danger' : 'bg-primary' %>">
                      <%= user.role ? user.role.name : (user.roleId === 1 ? 'Admin' : 'User') %>
                    </span>
                  </td>
                  <td><%= user.membership ? user.membership.name : 'Basic' %></td>
                  <td><%= new Date(user.createdAt).toLocaleDateString() %></td>
                  <td>
                    <div class="btn-group btn-group-sm" role="group">
                      <button type="button" class="btn btn-info" data-bs-toggle="modal" data-bs-target="#viewUserModal<%= user.id %>">
                        <i class="bi bi-eye"></i> View
                      </button>
                      <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#editUserModal<%= user.id %>">
                        <i class="bi bi-pencil"></i> Edit
                      </button>
                    </div>

                    <!-- View User Modal -->
                    <div class="modal fade" id="viewUserModal<%= user.id %>" tabindex="-1" aria-labelledby="viewUserModalLabel<%= user.id %>" aria-hidden="true">
                      <div class="modal-dialog">
                        <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
                          <div class="modal-header">
                            <h5 class="modal-title" id="viewUserModalLabel<%= user.id %>">User Details</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                          </div>
                          <div class="modal-body">
                            <div class="mb-3">
                              <strong>ID:</strong> <%= user.id %>
                            </div>
                            <div class="mb-3">
                              <strong>Username:</strong> <%= user.username %>
                            </div>
                            <div class="mb-3">
                              <strong>Name:</strong> <%= user.firstname %> <%= user.lastname %>
                            </div>
                            <div class="mb-3">
                              <strong>Email:</strong> <%= user.email %>
                            </div>
                            <div class="mb-3">
                              <strong>Role:</strong> 
                              <span class="badge <%= user.roleId === 1 ? 'bg-danger' : 'bg-primary' %>">
                                <%= user.role ? user.role.name : (user.roleId === 1 ? 'Admin' : 'User') %>
                              </span>
                            </div>
                            <div class="mb-3">
                              <strong>Membership:</strong> <%= user.membership ? user.membership.name : 'Basic' %>
                            </div>
                            <div class="mb-3">
                              <strong>Address:</strong> <%= user.address || 'Not provided' %>
                            </div>
                            <div class="mb-3">
                              <strong>Phone:</strong> <%= user.phone || 'Not provided' %>
                            </div>
                            <div class="mb-3">
                              <strong>Joined:</strong> <%= new Date(user.createdAt).toLocaleString() %>
                            </div>
                          </div>
                          <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                          </div>
                        </div>
                      </div>
                    </div>

                    <!-- Edit User Modal -->
                    <div class="modal fade" id="editUserModal<%= user.id %>" tabindex="-1" aria-labelledby="editUserModalLabel<%= user.id %>" aria-hidden="true">
                      <div class="modal-dialog">
                        <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
                          <div class="modal-header">
                            <h5 class="modal-title" id="editUserModalLabel<%= user.id %>">Edit User</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                          </div>
                          <div class="modal-body">
                            <form action="/users/update/<%= user.id %>" method="POST">
                              <div class="mb-3">
                                <label for="roleId<%= user.id %>" class="form-label">Role</label>
                                <select class="form-select" id="roleId<%= user.id %>" name="roleId" style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                                  <option value="1" <%= user.roleId === 1 ? 'selected' : '' %>>Admin</option>
                                  <option value="2" <%= user.roleId === 2 ? 'selected' : '' %>>User</option>
                                </select>
                              </div>
                              <div class="mb-3">
                                <label for="membershipId<%= user.id %>" class="form-label">Membership</label>
                                <select class="form-select" id="membershipId<%= user.id %>" name="membershipId" style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                                  <% if (locals.memberships && memberships.length > 0) { %>
                                    <% memberships.forEach(membership => { %>
                                      <option value="<%= membership.id %>" <%= user.membershipId === membership.id ? 'selected' : '' %>><%= membership.name %> (<%= membership.discount %>% discount)</option>
                                    <% }) %>
                                  <% } else { %>
                                    <option value="1">Basic (0% discount)</option>
                                  <% } %>
                                </select>
                              </div>
                              <div class="text-end">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                <button type="submit" class="btn btn-danger" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Save Changes</button>
                              </div>
                            </form>
                          </div>
                        </div>
                      </div>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        <% } else { %>
          <div class="alert alert-info" style="background-color: rgba(128, 128, 128, 0.515); color: red; border: 1px solid black;">
            No users found.
          </div>
        <% } %>
      </div>
    </div>
  </div>
  </div>

  <!-- MEMBERSHIPS TAB -->
  <div class="tab-pane fade" id="memberships" role="tabpanel" aria-labelledby="memberships-tab">
  <div class="card mt-3" style="background-color: rgb(79, 74, 74); color: red; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000; border-radius: 15px;">
    <div class="card-header d-flex justify-content-between align-items-center">
      <h3>Membership Management</h3>
      <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#addMembershipModal" style="color: red; border: 1px solid black; font-weight: bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">
        Add New Membership
      </button>
    </div>
    <div class="card-body">
      <!-- Memberships table -->
      <div class="table-responsive">
        <% if (locals.memberships && memberships.length > 0) { %>
          <table class="table table-dark table-hover">
            <thead>
              <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Discount</th>
                <th>User Count</th>
                <th width="150">Actions</th>
              </tr>
            </thead>
            <tbody>
              <% memberships.forEach(membership => { %>
                <tr>
                  <td><%= membership.id %></td>
                  <td><%= membership.name %></td>
                  <td><%= membership.discount %>%</td>
                  <td><%= membership.userCount || 0 %></td>
                  <td>
                    <div class="btn-group btn-group-sm" role="group">
                      <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#editMembershipModal<%= membership.id %>">
                        <i class="bi bi-pencil"></i> Edit
                      </button>
                      <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteMembershipModal<%= membership.id %>" <%= membership.id === 1 ? 'disabled' : '' %>>
                        <i class="bi bi-trash"></i> Delete
                      </button>
                    </div>

                    <!-- Edit Membership Modal -->
                    <div class="modal fade" id="editMembershipModal<%= membership.id %>" tabindex="-1" aria-labelledby="editMembershipModalLabel<%= membership.id %>" aria-hidden="true">
                      <div class="modal-dialog">
                        <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
                          <div class="modal-header">
                            <h5 class="modal-title" id="editMembershipModalLabel<%= membership.id %>">Edit Membership</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                          </div>
                          <div class="modal-body">
                            <form action="/memberships/edit/<%= membership.id %>" method="POST">
                              <div class="mb-3">
                                <label for="name<%= membership.id %>" class="form-label">Name</label>
                                <input type="text" class="form-control" id="name<%= membership.id %>" name="name" value="<%= membership.name %>" required <%= membership.id === 1 ? 'readonly' : '' %> style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                                <% if(membership.id === 1) { %>
                                  <small class="text-muted">Basic membership name cannot be changed</small>
                                <% } %>
                              </div>
                              <div class="mb-3">
                                <label for="discount<%= membership.id %>" class="form-label">Discount (%)</label>
                                <input type="number" class="form-control" id="discount<%= membership.id %>" name="discount" min="0" max="100" value="<%= membership.discount %>" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                              </div>
                              <div class="text-end">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                <button type="submit" class="btn btn-danger" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Save Changes</button>
                              </div>
                            </form>
                          </div>
                        </div>
                      </div>
                    </div>

                    <!-- Delete Membership Modal -->
                    <div class="modal fade" id="deleteMembershipModal<%= membership.id %>" tabindex="-1" aria-labelledby="deleteMembershipModalLabel<%= membership.id %>" aria-hidden="true">
                      <div class="modal-dialog">
                        <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
                          <div class="modal-header">
                            <h5 class="modal-title" id="deleteMembershipModalLabel<%= membership.id %>">Confirm Delete</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                          </div>
                          <div class="modal-body">
                            <p>Are you sure you want to delete the membership "<%= membership.name %>"?</p>
                            <% if (membership.userCount && membership.userCount > 0) { %>
                              <div class="alert alert-warning" style="background-color: rgba(255, 193, 7, 0.2); color: red; border: 1px solid #ffc107;">
                                Warning: This membership has <%= membership.userCount %> users. They will be moved to Basic membership if you delete this.
                              </div>
                            <% } %>
                          </div>
                          <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                            <form action="/memberships/delete/<%= membership.id %>" method="POST">
                              <button type="submit" class="btn btn-danger" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Delete</button>
                            </form>
                          </div>
                        </div>
                      </div>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        <% } else { %>
          <div class="alert alert-info" style="background-color: rgba(128, 128, 128, 0.515); color: red; border: 1px solid black;">
            No memberships found.
          </div>
        <% } %>
      </div>
    </div>
  </div>
  </div>


  <!-- MODALS -->

  <!-- Product Modal -->
  <div class="modal fade" id="addProductModal" tabindex="-1" aria-labelledby="addProductModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
        <div class="modal-header">
          <h5 class="modal-title" id="addProductModalLabel">Add New Product</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <form action="/products/add" method="POST">
            <div class="mb-3">
              <label for="name" class="form-label">Name</label>
              <input type="text" class="form-control" id="name" name="name" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
            </div>
            <div class="mb-3">
              <label for="description" class="form-label">Description</label>
              <textarea class="form-control" id="description" name="description" rows="3" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;"></textarea>
            </div>
            <div class="mb-3">
              <label for="price" class="form-label">Price</label>
              <input type="number" class="form-control" id="price" name="price" step="0.01" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
            </div>
            <div class="mb-3">
              <label for="quantity" class="form-label">Quantity</label>
              <input type="number" class="form-control" id="quantity" name="quantity" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
            </div>
            <div class="mb-3">
              <label for="imgUrl" class="form-label">Image URL</label>
              <input type="text" class="form-control" id="imgUrl" name="imgUrl" style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
            </div>
            <div class="mb-3">
              <label for="brandId" class="form-label">Brand</label>
              <select class="form-select" id="brandId" name="brandId" style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                <% if (locals.brands && brands.length > 0) { %>
                  <% brands.forEach(brand => { %>
                    <option value="<%= brand.id %>"><%= brand.name %></option>
                  <% }) %>
                <% } else { %>
                  <option value="">No brands available</option>
                <% } %>
              </select>
            </div>
            <div class="mb-3">
              <label for="categoryId" class="form-label">Category</label>
              <select class="form-select" id="categoryId" name="categoryId" style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
                <% if (locals.categories && categories.length > 0) { %>
                  <% categories.forEach(category => { %>
                    <option value="<%= category.id %>"><%= category.name %></option>
                  <% }) %>
                <% } else { %>
                  <option value="">No categories available</option>
                <% } %>
              </select>
            </div>
            <div class="text-end">
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
              <button type="submit" class="btn btn-danger" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Add Product</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>


  <!-- Category Modal -->
  <div class="modal fade" id="addCategoryModal" tabindex="-1" aria-labelledby="addCategoryModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
        <div class="modal-header">
          <h5 class="modal-title" id="addCategoryModalLabel">Add New Category</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <form action="/categories/add" method="POST">
            <div class="mb-3">
              <label for="name" class="form-label">Name</label>
              <input type="text" class="form-control" id="name" name="name" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
            </div>
            <div class="text-end">
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
              <button type="submit" class="btn btn-danger" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Add Category</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>


  <!-- Brand Modal -->
  <div class="modal fade" id="addBrandModal" tabindex="-1" aria-labelledby="addBrandModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
      <div class="modal-header">
        <h5 class="modal-title" id="addBrandModalLabel">Add New Brand</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <form action="/brands/add" method="POST">
          <div class="mb-3">
            <label for="brandName" class="form-label">Name</label>
            <input type="text" class="form-control" id="brandName" name="name" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
          </div>
          <div class="text-end">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
            <button type="submit" class="btn btn-danger" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Add Brand</button>
          </div>
        </form>
      </div>
    </div>
  </div>
  </div>


  <!-- Membership Modal -->
  <div class="modal fade" id="addMembershipModal" tabindex="-1" aria-labelledby="addMembershipModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content" style="background-color: rgb(79, 74, 74); color: red;">
      <div class="modal-header">
        <h5 class="modal-title" id="addMembershipModalLabel">Add New Membership</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <form action="/memberships/add" method="POST">
          <div class="mb-3">
            <label for="membershipName" class="form-label">Name</label>
            <input type="text" class="form-control" id="membershipName" name="name" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
          </div>
          <div class="mb-3">
            <label for="membershipDiscount" class="form-label">Discount (%)</label>
            <input type="number" class="form-control" id="membershipDiscount" name="discount" min="0" max="100" value="0" required style="color: red; background-color: rgba(128, 128, 128, 0.515); border: 1px solid black;">
          </div>
          <div class="text-end">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
            <button type="submit" class="btn btn-danger" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Add Membership</button>
          </div>
        </form>
      </div>
    </div>
  </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  <script>

      // Advanced search functionality
  document.getElementById('advancedSearchForm').addEventListener('submit', async function(event) {
    event.preventDefault();
    
    const query = document.getElementById('searchQuery').value;
    const type = document.getElementById('searchType').value;
    const resultsDiv = document.getElementById('searchResults');
    const resultCountSpan = document.getElementById('resultCount');
    const tableBody = document.querySelector('#products table tbody');
    const originalTableContent = tableBody.innerHTML;
    
    if (!query) {
      alert('Please enter a search term');
      return;
    }
    
    try {
      // Get the token from the session
      const token = '<%= token %>';
      
      const response = await fetch('/api/search', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Authorization': `Bearer ${token}`
        },
        body: JSON.stringify({ query, type })
      });
      
      const data = await response.json();
      
      if (data.status === 'success') {
        const products = data.data.products;
        resultCountSpan.textContent = data.data.count;
        resultsDiv.style.display = 'block';
        
        // Clear and update the table
        tableBody.innerHTML = '';
        
        products.forEach(product => {
          const row = document.createElement('tr');
          row.innerHTML = `
            <td>${product.id}</td>
            <td>${product.name}</td>
            <td>${product.description.substring(0, 50)}${product.description.length > 50 ? '...' : ''}</td>
            <td>$${product.price.toFixed(2)}</td>
            <td>${product.quantity}</td>
            <td>${product.brand.name}</td>
            <td>${product.category.name}</td>
            <td>
              <div class="btn-group">
                <button type="button" class="btn btn-sm btn-danger" data-bs-toggle="modal" data-bs-target="#editProductModal${product.id}" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Edit</button>
                <button type="button" class="btn btn-sm btn-danger" data-bs-toggle="modal" data-bs-target="#deleteProductModal${product.id}" style="color: red; border: 1px solid black; font-weight: Bold; text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">Delete</button>
              </div>
            </td>
          `;
          tableBody.appendChild(row);
        });
      } else {
        alert(`Search error: ${data.data.Result}`);
      }
    } catch (error) {
      console.error('Error performing search:', error);
      alert('An error occurred while searching');
    }
  });
  
  // Clear search results and restore original table
  document.getElementById('clearSearchResults').addEventListener('click', function() {
    document.getElementById('searchResults').style.display = 'none';
    document.getElementById('searchQuery').value = '';
    
    // Restore original product list by triggering the existing product search with empty value
    const productSearchInput = document.getElementById('productSearch');
    productSearchInput.value = '';
    productSearchInput.dispatchEvent(new Event('keyup'));
  });





    // Product search functionality
    document.getElementById('productSearch').addEventListener('keyup', function() {
      const searchTerm = this.value.toLowerCase();
      const tableRows = document.querySelectorAll('#products table tbody tr');
      
      tableRows.forEach(row => {
        const name = row.cells[1].textContent.toLowerCase();
        const description = row.cells[2].textContent.toLowerCase();
        const brand = row.cells[5].textContent.toLowerCase();
        const category = row.cells[6].textContent.toLowerCase();
        
        if (name.includes(searchTerm) || description.includes(searchTerm) || 
            brand.includes(searchTerm) || category.includes(searchTerm)) {
          row.style.display = '';
        } else {
          row.style.display = 'none';
        }
      });
    });

    // Category search functionality
    document.getElementById('categorySearch').addEventListener('keyup', function() {
      const searchTerm = this.value.toLowerCase();
      const tableRows = document.querySelectorAll('#categories table tbody tr');

      tableRows.forEach(row => {
        const name = row.cells[1].textContent.toLowerCase();

        if (name.includes(searchTerm)) {
          row.style.display = '';
        } else {
          row.style.display = 'none';
        }
      });
    });

    // Brand search functionality
    document.getElementById('brandSearch').addEventListener('keyup', function() {
    const searchTerm = this.value.toLowerCase();
    const tableRows = document.querySelectorAll('#brands table tbody tr');

    tableRows.forEach(row => {
      const name = row.cells[1].textContent.toLowerCase();

      if (name.includes(searchTerm)) {
        row.style.display = '';
      } else {
        row.style.display = 'none';
      }
    });
  });

    // Order search and filter functionality
    document.addEventListener('DOMContentLoaded', function() {
      // Order status filter
      const orderFilterBtns = document.querySelectorAll('[data-filter]');
      orderFilterBtns.forEach(btn => {
        btn.addEventListener('click', function() {
          // Update active button
          orderFilterBtns.forEach(b => b.classList.remove('active'));
          this.classList.add('active');
          
          const filter = this.getAttribute('data-filter');
          const orderRows = document.querySelectorAll('#orders table tbody tr');
          
          orderRows.forEach(row => {
            if (filter === 'all' || row.getAttribute('data-status') === filter) {
              row.style.display = '';
            } else {
              row.style.display = 'none';
            }
          });
        });
      });
      
      // Order search functionality
      document.getElementById('orderSearch')?.addEventListener('keyup', function() {
        const searchTerm = this.value.toLowerCase();
        const orderRows = document.querySelectorAll('#orders table tbody tr');
        
        orderRows.forEach(row => {
          const orderId = row.cells[0].textContent.toLowerCase();
          const user = row.cells[1].textContent.toLowerCase();
          const status = row.cells[3].textContent.toLowerCase();
          
          if (orderId.includes(searchTerm) || 
              user.includes(searchTerm) || 
              status.includes(searchTerm)) {
            row.style.display = '';
          } else {
            row.style.display = 'none';
          }
        });
      });
    });

    // User search functionality
    document.getElementById('userSearch')?.addEventListener('keyup', function() {
      const searchTerm = this.value.toLowerCase();
      const tableRows = document.querySelectorAll('#users table tbody tr');

      tableRows.forEach(row => {
        const username = row.cells[1].textContent.toLowerCase();
        const name = row.cells[2].textContent.toLowerCase();
        const email = row.cells[3].textContent.toLowerCase();

        if (username.includes(searchTerm) || 
            name.includes(searchTerm) || 
            email.includes(searchTerm)) {
          row.style.display = '';
        } else {
          row.style.display = 'none';
        }
      });
    });

  </script>
</body>
</html>